module.exports = ({ env }) => ({
    // Content Manager plugin
    "content-manager": {
      enabled: true,
      // Add any custom configuration options here
    },
    // Roles & Permissions plugin
  "users-permissions": {
    enabled: true,
    // Add any custom configuration options here
  },
  });
  